# Worker principal : ikcp-prospect (pipeline prospects → Notion + emails)
name = "ikcp-prospect"
main = "index.js"
compatibility_date = "2024-01-01"
compatibility_flags = ["nodejs_compat"]

[vars]
NOTIFY_EMAIL = "maxime@ikcp.eu"

# Secrets à configurer via Cloudflare Dashboard (Settings → Variables and Secrets) :
# wrangler secret put NOTION_TOKEN
# wrangler secret put NOTION_DB_ID
