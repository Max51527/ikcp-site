name: Deploy Cloudflare Workers

on:
  push:
    branches: [main]
    paths:
      - 'workers/ikcp-marcel/**'
      - 'workers/ikcp-voice/**'
      - 'workers/ikcp-batisseur/**'
      - 'workers/ikcp-hermes/**'
      - 'workers/ikcp-veille/**'
      - 'workers/ikcp-lifestyle/**'
      - 'workers/ikcp-client/**'
      - 'workers/ikcp-pappers/**'
      - 'workers/ikcp-cms-auth/**'
  workflow_dispatch:
    inputs:
      target:
        description: 'Worker à déployer'
        required: false
        default: 'all'
        type: choice
        options:
          - all
          - ikcp-marcel
          - ikcp-voice
          - ikcp-batisseur
          - ikcp-hermes
          - ikcp-veille
          - ikcp-lifestyle
          - ikcp-client
          - ikcp-pappers
          - ikcp-cms-auth

jobs:
  deploy-ikcp-marcel:
    name: Deploy ikcp-chat (Marcel)
    runs-on: ubuntu-latest
    if: >
      github.event_name == 'workflow_dispatch' &&
      (github.event.inputs.target == 'all' || github.event.inputs.target == 'ikcp-marcel') ||
      github.event_name == 'push'
    steps:
      - uses: actions/checkout@v4

      - name: Deploy ikcp-chat (Marcel chef d'orchestre)
        uses: cloudflare/wrangler-action@v3
        env:
          FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: 'true'
        with:
          apiToken: ${{ secrets.CF_API_TOKEN }}
          accountId: eaddc4cc77d99dd397a62e5d5a1b6864
          workingDirectory: workers/ikcp-marcel

  deploy-ikcp-voice:
    name: Deploy ikcp-voice (TTS/STT Marcel)
    runs-on: ubuntu-latest
    if: >
      github.event_name == 'workflow_dispatch' &&
      (github.event.inputs.target == 'all' || github.event.inputs.target == 'ikcp-voice') ||
      github.event_name == 'push'
    steps:
      - uses: actions/checkout@v4

      - name: Deploy ikcp-voice (ElevenLabs + Voxtral)
        uses: cloudflare/wrangler-action@v3
        env:
          FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: 'true'
        with:
          apiToken: ${{ secrets.CF_API_TOKEN }}
          accountId: eaddc4cc77d99dd397a62e5d5a1b6864
          workingDirectory: workers/ikcp-voice

  deploy-ikcp-batisseur:
    name: "Deploy ikcp-batisseur (Patrimoine 360\xB0)"
    runs-on: ubuntu-latest
    if: >
      github.event_name == 'workflow_dispatch' &&
      (github.event.inputs.target == 'all' || github.event.inputs.target == 'ikcp-batisseur') ||
      github.event_name == 'push'
    steps:
      - uses: actions/checkout@v4

      - name: "Deploy ikcp-batisseur (B\xE2tisseur Opus 4.7)"
        uses: cloudflare/wrangler-action@v3
        env:
          FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: 'true'
        with:
          apiToken: ${{ secrets.CF_API_TOKEN }}
          accountId: eaddc4cc77d99dd397a62e5d5a1b6864
          workingDirectory: workers/ikcp-batisseur

  deploy-ikcp-hermes:
    name: "Deploy ikcp-hermes (Transmission patrimoniale)"
    runs-on: ubuntu-latest
    if: >
      github.event_name == 'workflow_dispatch' &&
      (github.event.inputs.target == 'all' || github.event.inputs.target == 'ikcp-hermes') ||
      github.event_name == 'push'
    steps:
      - uses: actions/checkout@v4

      - name: "Deploy ikcp-hermes (Herm\xE8s Opus 4.7)"
        uses: cloudflare/wrangler-action@v3
        env:
          FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: 'true'
        with:
          apiToken: ${{ secrets.CF_API_TOKEN }}
          accountId: eaddc4cc77d99dd397a62e5d5a1b6864
          workingDirectory: workers/ikcp-hermes

  deploy-ikcp-veille:
    name: "Deploy ikcp-veille (Veille Perplexity)"
    runs-on: ubuntu-latest
    if: >
      github.event_name == 'workflow_dispatch' &&
      (github.event.inputs.target == 'all' || github.event.inputs.target == 'ikcp-veille') ||
      github.event_name == 'push'
    steps:
      - uses: actions/checkout@v4

      - name: "Deploy ikcp-veille (Perplexity Pro Proxy)"
        uses: cloudflare/wrangler-action@v3
        env:
          FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: 'true'
        with:
          apiToken: ${{ secrets.CF_API_TOKEN }}
          accountId: eaddc4cc77d99dd397a62e5d5a1b6864
          workingDirectory: workers/ikcp-veille

  deploy-ikcp-lifestyle:
    name: "Deploy ikcp-lifestyle (8 agents Sonnet)"
    runs-on: ubuntu-latest
    if: >
      github.event_name == 'workflow_dispatch' &&
      (github.event.inputs.target == 'all' || github.event.inputs.target == 'ikcp-lifestyle') ||
      github.event_name == 'push'
    steps:
      - uses: actions/checkout@v4

      - name: "Deploy ikcp-lifestyle (Architecte/Curateur/Concierge/Stratege...)"
        uses: cloudflare/wrangler-action@v3
        env:
          FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: 'true'
        with:
          apiToken: ${{ secrets.CF_API_TOKEN }}
          accountId: eaddc4cc77d99dd397a62e5d5a1b6864
          workingDirectory: workers/ikcp-lifestyle

  deploy-ikcp-client:
    name: "Deploy ikcp-client (Auth + tiers + quotas)"
    runs-on: ubuntu-latest
    if: >
      github.event_name == 'workflow_dispatch' &&
      (github.event.inputs.target == 'all' || github.event.inputs.target == 'ikcp-client') ||
      github.event_name == 'push'
    steps:
      - uses: actions/checkout@v4

      - name: "Deploy ikcp-client (portail membre freemium)"
        uses: cloudflare/wrangler-action@v3
        env:
          FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: 'true'
        with:
          apiToken: ${{ secrets.CF_API_TOKEN }}
          accountId: eaddc4cc77d99dd397a62e5d5a1b6864
          workingDirectory: workers/ikcp-client

  deploy-ikcp-pappers:
    name: "Deploy ikcp-pappers (Cartographie SIREN)"
    runs-on: ubuntu-latest
    if: >
      github.event_name == 'workflow_dispatch' &&
      (github.event.inputs.target == 'all' || github.event.inputs.target == 'ikcp-pappers') ||
      github.event_name == 'push'
    steps:
      - uses: actions/checkout@v4

      - name: "Deploy ikcp-pappers (RNE / Pappers)"
        uses: cloudflare/wrangler-action@v3
        env:
          FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: 'true'
        with:
          apiToken: ${{ secrets.CF_API_TOKEN }}
          accountId: eaddc4cc77d99dd397a62e5d5a1b6864
          workingDirectory: workers/ikcp-pappers

  deploy-ikcp-cms-auth:
    name: "Deploy ikcp-cms-auth (OAuth GitHub pour le CMS)"
    runs-on: ubuntu-latest
    if: >
      github.event_name == 'workflow_dispatch' &&
      (github.event.inputs.target == 'all' || github.event.inputs.target == 'ikcp-cms-auth') ||
      github.event_name == 'push'
    steps:
      - uses: actions/checkout@v4

      - name: "Deploy ikcp-cms-auth (proxy OAuth Sveltia CMS)"
        uses: cloudflare/wrangler-action@v3
        env:
          FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: 'true'
        with:
          apiToken: ${{ secrets.CF_API_TOKEN }}
          accountId: eaddc4cc77d99dd397a62e5d5a1b6864
          workingDirectory: workers/ikcp-cms-auth
